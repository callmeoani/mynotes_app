// ============== || PACKAGES IMPORTS || ==================

// ============== || ICONS IMPORTS || ==================

// ============== || IMAGES IMPORTS || ==================

// ============== || FUNCTIONS/UTILITIES IMPORTS || ==================

// ============== || HOOKS IMPORTS || ==================

// ============== || COMPONENTS IMPORTS || ==================
import "./NewNote.css";

const NewNote = () => {
  return (
    <>
      <div className="NewNote">This is the new note page</div>
    </>
  );
};

export default NewNote;
